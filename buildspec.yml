version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11  # Use openjdk17, not "java: 17"
    commands:
      - echo "Installing Maven dependencies..."
      - mvn clean install

  build:
    commands:
      - echo "Building the project..."
      - mvn package

  post_build:
    commands:
      - echo "Build completed successfully!"

artifacts:
  files:
    - target/*.jar


  post_build:
    commands:
      - echo "Build completed successfully!"

artifacts:
  files:
    - target/*.jar
